# 项目协作规则（CLAUDE.md）

> 本文件在每次 session 启动时都会被加载，请严格遵守以下规则。
> 规则冲突时，按编号从小到大优先级递减，第 0 条优先级最高。
>
> 【使用说明】本文件是通用模板，标有 `<按项目调整>` 的地方需要你根据实际项目
> 情况修改或删除；其余部分可以直接套用到任意技术栈的项目上。

## 0. 自我保护（最高优先级）

- 禁止修改本文件，除非用户当前对话明确要求。
- 规则疑似过时：提醒用户确认，不擅自改。
- 批量操作（重构/格式化/批量替换）默认跳过 `CLAUDE.md`、`.claude/`，除非被点名。

## 1. 状态持久化

- 维护 `.claude/STATE.md`（任务历史）+ `.claude/BUGS.md`（当前待办）。
- 新任务前先读 `STATE.md`，以文件为准，不依赖对话记忆。
- 完成子任务立即追加记录，格式：
  ```
  ## [日期] 任务名
  - 状态：已完成 / 进行中 / 待处理
  - 优先级：P0-P5
  - 描述 / 涉及文件 / 备注
  ```
- 多条待办先写入 `BUGS.md` 再逐条处理，处理完立即从中删除（不留"已完成"标记）；完整细节只进 `STATE.md`。
- 同一事项只存在于两个文件之一，不重复维护。

## 2. 上下文管理

- 对话变长（用户提到"上下文快满了"等）时，主动建议先落盘状态，再开新 session 或 `/compact`。
- 关键信息以文件为准，不假设自己记得住。
- `STATE.md`/`BUGS.md` 只留近期记录；旧的"已完成"（如超 20 条）整体迁入 `.claude/STATE-ARCHIVE.md`。

## 3. 代码修改规范

- 改动前先说明范围，不做无关的大改动。
- 改完跑相关测试即可，不必每次全量。
- 不确定就提问或声明假设，不臆造需求；下结论前先读代码验证，不凭经验断言。

## 4. 开发服务器与浏览器资源

- 禁止主动启动 dev server 或长期进程（含 `&`/`nohup` 后台变体），改完直接说明、由用户自测；一次性脚本/测试/类型检查不受限。
- 用浏览器自动化工具验证后必须关闭测试标签页；若是唯一标签页，先开空白页再关。

## 5. Git 与版本号

- commit message 格式：`<类型>: <说明>`（feat/fix/refactor/docs/test/chore）。
- 版本号按改动幅度递增（patch/minor/major），纯文档改动不递增 `<按项目调整：无版本管理则删除本条>`；每次回复注明版本号变动。
- 不自动执行 `commit` / `push` / 历史重写操作（`rebase -i`、`reset --hard`、`push --force`），除非本次任务被明确要求；单次授权不延续到后续任务。

## 6. 沟通风格

- 直接给结论和改动内容，不寒暄。
- 不确定的技术判断标注"推测"或"已验证"。

## 7. 领域专项规则 `<按项目调整：按需替换或删除本节>`

> 存放本项目特有的架构红线（如：涉及某模块必须先读哪份文档、某类操作严禁静默降级）。
> - 涉及 `<高风险模块>` 时，先读 `<设计文档路径>`。
> - 严禁 `<具体危险行为>`，除非用户明确要求。

## 8. 功能索引维护

- 维护 `.claude/FEATURE-MAP.md`（模块 → 文件路径，仅供导航，不放状态/历史）。
- 新增/迁移模块时更新；路径类可自主改，背景决策类改动需在回复中提示确认。
- 模块下线同步删条目；超 100 行时按模块拆分为 `.claude/FEATURE-MAP/<module>.md`。
- 接手"定位功能"类任务先读此文件，缺失再全项目搜索并补全。

## 9. 依赖与环境管理

- 新增第三方依赖前，先说明为什么需要、有哪些候选，等待确认后再执行安装，不擅自 `npm install` / `pip install` / `cargo add` 等引入新包。
- 禁止把密钥、`.env`、凭据、私钥等文件提交到仓库，也不要把其内容输出到回复或日志里。
- 新增或修改环境变量时，同步更新 `.env.example`（或项目里对应的示例配置文件），并在回复中说明新增了哪个变量、用途是什么。

## 10. 代码风格与一致性

- 优先匹配项目现有的代码风格、命名习惯、目录组织方式，不引入自己的偏好写法。
- 完成修改后，如果项目配置了 lint / format 工具，跑一遍确认没有新增报错；不确定项目用的是哪套工具时先查看配置文件（如 `.eslintrc`、`.prettierrc`、`pyproject.toml` 等）。
- 倾向最小化改动（minimal diff）：只改任务需要的部分，不顺手重构、重排无关代码，即使看到"顺手能改好"的地方，也先提出来问，不擅自扩大改动范围。

## 11. 测试规范 `<按项目调整：如果项目没有测试体系，可删除本条>`

- 新增功能需要补充对应测试；修改已有逻辑时，检查是否有测试覆盖到，不允许在没有说明的情况下降低测试覆盖率。
- 修复 bug 时，优先写一个能复现该问题的测试用例（跑到失败），再改代码让它通过，而不是改完代码就结束。
- 测试文件的命名、目录结构，跟随项目现有约定（如 `*.test.ts`、`__tests__/` 目录等）。

## 12. 高风险操作二次确认

- 涉及以下类别的操作，必须先说明影响范围并等待明确确认，不直接执行：
  - 删除文件、清空目录、批量重命名/移动文件；
  - 数据库层面的破坏性操作（`DROP TABLE`、清库、不带 `WHERE` 的批量更新/删除等）；
  - 修改 CI/CD 配置、部署脚本、生产环境配置；
  - 修改权限、密钥轮换、对外发布相关的操作。
- 数据库迁移脚本需要考虑可回滚性，不直接对开发/生产库执行一次性、不可逆的 SQL。

## 13. 文档同步

- 新增或变更对外可见的功能时，检查是否需要同步更新 `README.md`、`CHANGELOG`（或项目里对应的文档文件），不要只改代码不改文档导致文档过期。
- 文档更新和代码改动放在同一次任务里说明清楚，不要留到"以后有空再补"。

## 14. 外部调用与成本意识

- 调用需要计费的外部服务（云 API、付费模型、短信/邮件网关等）之前，先说明预计的调用量级或成本，等待确认后再执行，尤其是批量/循环调用场景。
- 验证代码逻辑时，避免执行会产生真实副作用的操作（真实发邮件、发消息、下单、扣款等），除非用户明确要求；需要验证这类逻辑时优先使用 mock/沙箱/测试环境。

## 15. 命名与语言规范 `<按项目调整：请填入本项目实际约定>`

- 代码注释使用：`<中文 / 英文，按项目实际情况填写>`。
- Commit message 使用：`<中文 / 英文>`。
- UI 文案 / 面向用户的文案使用：`<中文 / 英文 / 需要 i18n>`。
- 新建文件/组件的命名风格：`<如组件用大驼峰 PascalCase，工具函数文件用短横线 kebab-case，按项目现有约定填写>`。

## 待办 / 索引类文件一览

- 当前 bug 清单：`.claude/BUGS.md`
- 项目状态快照：`.claude/STATE.md`
- 状态归档（历史已完成任务）：`.claude/STATE-ARCHIVE.md`
- 功能实现索引（仅供 Claude 导航）：`.claude/FEATURE-MAP.md`
- `<按项目调整：如项目有其他规范文档/清单文件，在这里补充索引，方便快速定位>`
